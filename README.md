brisjs.github.io
====================

The https://brisjs.org/ website.

Push to master will automagically publish changes to [Netlify](http://netlify.com/).

> **Migration in progress** (see `docs/INDEX.md` + `docs/BUILD_WORKFLOW.md`): the repo root is
> the legacy site, kept live until cutover (ADR-006). The new **Astro frontend** lives in
> `web/` and the **Strapi CMS** in `cms/`.
